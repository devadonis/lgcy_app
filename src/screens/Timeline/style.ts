import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    timelineButton: {
        borderColor: '#000',
        borderWidth: 1,
        borderRadius: 12,
        paddingHorizontal: 60,
        paddingVertical: 8,
        marginVertical: 10
    },
    timelineButtonText: {
        color: '#000',
        fontSize: 16,
        fontWeight: '500',
    }
});